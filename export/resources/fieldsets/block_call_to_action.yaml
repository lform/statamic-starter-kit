title: "Block - Call to Action"
fields:
    - handle: call_to_action
      field:
          max_items: 1
          mode: select
          collections:
              - calls_to_action
          type: entries
          display: "Call to Action"
          instructions: "If left empty, no CTA will be displayed."
          instructions_position: below
          sortable: false
          replicator_preview: false
